name: Check Versions

on:  
  push:
    branches: [ "main" ]
  workflow_dispatch:
    inputs:
      test-group: 
        type: choice
        description: 'Run only test group'             
        options: 
        - "--help"
        - "--version"
      build-before-test:
        type: boolean
        description: 'Build before tests'
        default: true
        required: true


jobs:
  check-versions:
    runs-on: ubuntu-latest    
    # container:
    #   image: docker/github-actions

    steps:
#      - name: sls install
#        run: npm install -g serverless

#      - name: sls version
#        run: serverless --version

      - name: info
        run: |
          echo "test-group= ${{ github.event.inputs.test-group }} "
          echo "build-before-test= ${{ github.event.inputs.build-before-test }} "
          
      - name: build-before-test is not false
        if: github.event.inputs.build-before-test != false
        run: |
          echo "build-before-test is true "

      #- name: node prompt
      #  if: github.event.inputs.test-group != ''
      #  run: node ${{ github.event.inputs.test-group }} 

      #- name: node version
      #  if: github.event.inputs.test-group == ''
      #  run: node ${{ github.event.inputs.test-group }} 
